import styles from './messaging_page.module.scss';

const MessagingPage: React.FC = () => {
  return (
    <main className={styles.main}>
      <h1 className={styles.title}>Messaging</h1>
    </main>
  );
};

export default MessagingPage;
