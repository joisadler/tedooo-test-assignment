import styles from './notifications_page.module.scss';

const NotificationsPage: React.FC = () => {
  return (
    <main className={styles.main}>
      <h1 className={styles.title}>Notifications</h1>
    </main>
  );
};

export default NotificationsPage;
